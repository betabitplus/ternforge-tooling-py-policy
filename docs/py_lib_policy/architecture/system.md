# System

`_api` contains declaration/CLI/check facades only. `_internal/config` owns project metadata loading and discovery; `_internal/policy` owns syntax, package, project, and orchestration checks.

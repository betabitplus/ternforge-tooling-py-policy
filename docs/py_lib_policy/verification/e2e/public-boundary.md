# Public boundary

`tests/py_lib_policy/e2e/public_boundary/test_public_config_pipeline.py` verifies `py_lib_policy.check()` and the preserved top-level `main()` CLI boundary on a complete valid project fixture.

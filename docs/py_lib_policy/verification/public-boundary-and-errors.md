# Public boundary and errors

Unit tests pin the top-level exports; integration/property tests cover manifest validation; the e2e slice exercises both public Python and CLI entrypoints. Existing checker regression tests remain unchanged in scope.

# Tests

Policy tests live below `tests/py_lib_policy/` and exercise the public `py_lib_policy.check()` contract against temporary repository fixtures.

Run them with:

```bash
uv run pytest tests/py_lib_policy
```
